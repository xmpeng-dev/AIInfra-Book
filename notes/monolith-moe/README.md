# monolith-moe — MoE Super-Kernel + Comm-Compute Overlap (MI355X)

> **目标**：在 8×MI355X 上做 MoE 层的 **CCO（Communication-Computation Overlap）**，把 GEMM 计算与 All-to-All 通信融合在同一个 persistent kernel 内部，跨过 RCCL multi-stream 无法 overlap 的架构限制
> **平台**：8×MI355X (gfx950, XGMI 全互联)
> **栈**：PyTorch 2.12+rocm7.1 / ROCm 7.2, Triton (ByteDance fork), triton-distributed 3.4.0, primus_turbo CK
> **目标模型**：DeepSeek-V3 671B（256E EP8→32 local, top_k=8, H=7168, F=2048）

## 状态

| 维度 | 值 |
|---|---|
| **End-to-end super-kernel (DSV3 SPARSE, 8×MI355X)** | **4.82 ms / 598 TFLOPS / 1.76× vs PyTorch+RCCL 8.466 ms**（2026-05-13 Phase 2.1 SwiGLU pre-compute 落地后 8-GPU 实测，**512 t/g**）—— 比 Phase 2 6.27 ms 再缩 23%，比 README 旧 headline 7.95 ms / 363T / 1.07× 累计缩短 3.13 ms / +65% TFLOPS / +0.69× speedup |
| **Super-kernel @ 2048 t/g（训练实际工况）** | **26.5 ms / per call**（2026-05-13 per-step forward profile，super-kernel 占 MMOE forward 95%）—— 比 512 t/g 微基准 4× tokens 但 5.5× wall；vs baseline TEGroupedMLP ~7.8 ms / layer 慢 3.4×，是 MMOE forward 全部 56 ms overhead 的来源 |
| **Apples-to-apples vs PyTorch+RCCL（重测）** | **bench 单跑** 512 t/g 仍 1.46× ✓；**2048 t/g super-kernel 16.98 ms vs PyTorch 9.05 ms = 0.53× ❌**；**8192 t/g（生产）super-kernel 64.5 ms vs PyTorch 18.6 ms = 0.29× ❌**（PyTorch 在大 M 下 strongly super-linear，super-kernel slightly super-linear 但绝对值远落后）—— **之前所有优化都是在 512 t/g 调的，到训练工况全部反转** |
| **优化 cycle 1-8（本 session 进展）** | 训练工况 bench `T_src=2048 / 8192` 分别：cycle 1 baseline 14.43 / 56.08 ms (776 / 823 TFLOPS) → cycle 2 (256×256×64 tile) → cycle 3 (comm_ratio=0.250) → cycle 6 (re-profile) → cycle 7 (chunked-FC1, no win in bench) → **cycle 8 (parallel-peer copy) 13.09 / 50.87 ms (882 / 908 TFLOPS, -9.3 % both)**；vs PyTorch+RCCL T_src=2048 1.45× slower / T_src=8192 2.73× slower。剩余 wall：dispatch_wait 35 % / FC1 34 % / FC2 22 % / copy 6 % |
| **MMOE × Primus / DSV3 真实训练接入** | **20 iter e2e PASS + 三方 loss parity**（2026-05-13 方案 B / `examples/primus/dsv3_4layer_mmoe.yaml`，PP=1 EP=8 TP=1 4 层 3 MoE 层，mock data）：baseline TEGroupedMLP / MMOE eager_bwd / MMOE decomposed_bwd 三路同 seed 跑 20 iter，**max abs lm_loss diff 1.85e-3** (bf16 噪声)，grad_norm 各 iter 一致；性能：baseline 184 / eager 69 / **decomposed 107 TFLOP/s/GPU**（decomposed vs eager **+55%**）；端到端 wall：baseline 232 ms → eager 615 ms → **decomposed 400 ms** per iter |
| End-to-end super-kernel (TILE-FIT) | **3.39 ms / 243 TFLOPS**（2026-05-13 Phase 2.1 8-GPU 实测，vs Phase 2 3.86 ms / 213T -> −12% / +14%） |
| 调优脉络（DSV3 SPARSE wall） | 24.0 → 15.21 → 14.86 → 14.04 → **8.80**（P0 flat+small tile）→ **7.95**（P2 batched compute）→ **6.59**（PAD=8 + K_TILE=128）→ **6.27**（Phase 1 PAD=0+XOR swizzle）→ **6.27**（Phase 2 DTOLDS for FC1 + FC2-B）→ **4.82**（Phase 2.1 SwiGLU pre-compute → FC2 全 DTOLDS） |
| Hand-written Grouped GEMM (DSV3 grouped, standalone) | **GateUP 643T / Down 559T vs CK 1050T / 960T（0.60× / 0.58×）** —— DirectToLds + K-aligned dispatch landed 2026-05-12 21:30 |
| Layout C 设计 | 5 种 IPC buffer layout 对比完成，Layout C 最优（XGMI ~95% 效率 + 无 gather） |
| 选定方向 | **HIP C++ IPC**（已实现），参考 DeepEP `atomicAdd_system + ld_volatile_global` |
| 项目状态 | **重新激活中**（2026-05-12 起 P0/P1/P2 多轮优化已落地） |

## 进展时间线

| 日期 | 里程碑 | 关键数字 | 来源 note |
|---|---|---|---|
| 2026-07-07 09:20 | **最新 MoE 系统论文扫描（arxiv'26 + EuroSys'26，10 篇）** | 以 `ml-systems-papers` 为索引抓真实摘要：训练侧 MoEBlaze（>4×/-50% mem，co-design kernel + 消中间 buffer）、MegaScale-MoE（1.88×，op 级 comm-compute overlap，生产级）、MoE-DisCo；服务侧 PROBE / MixServe（fused AR-A2A）/ FineMoE；负载均衡 LLEP、容错 Tarragon、架构 LatentMoE（d→ℓ 砍 A2A 体积）/ DES。**结论**：op/collective 级 overlap 已被做到 1.88–3.8×，我们必须锁定 in-kernel chunk 级 GEMM×A2A 融合 + AMD/XGMI 空白区；FP8 + LatentMoE 式正交杠杆优先于继续动 compute 排布 | [latest_moe_papers_scan](./2026-07-07_latest-moe-systems-papers-scan.md) |
| 2026-05-14 12:10 | **PyTorch+RCCL MoE forward 算子级分解归档（8466 μs）** | 与 baseline **8.466 ms/layer** 对齐：overhead **189 μs (2.2%)** / comm **3669 μs (43.3%)** / compute **4607 μs (54.5%)**；明细：dispatch a2a 1427 + combine 2242，fc1 2727 + SwiGLU 161 + fc2 1719 | [operator_breakdown_8466](./2026-05-14_1210_operator_forward_breakdown_us_8466.md) |
| 2026-05-14 10:18 | **🔴 大幅校准 — cycle 1-14b 全部 bench 用错 workload；真实 DSV3 super-kernel 7.72 ms ≈ PyTorch+RCCL 7.60 ms (基本平)** | `tokens_per_gpu` 含义解读错误：`--tokens 8192` 是 source per GPU，但真实 DSV3 (global mbs=2 seq=4096) 的 source = 1024 per GPU。用 `--tokens 1024` 重测：super 7.72 ms vs PyTorch 7.60 ms = **0.98×**（之前认为的 0.36× / 2.7× 慢全错）。GEMM 实测 super 2.93 ms vs PyTorch 5.60 ms (FC1+FC2 比 PyTorch 快 **1.9×**)；瓶颈实际是 dispatch_wait 2.28 ms (PyTorch 0.90 ms = 我们多 +1.38 ms)。**Cycle 16 ROI 修正**：22× gap 的判断作废，packed_fc1 实测 1.95 ms vs old 1.76 ms，整合后净省 ~1.2 ms (不是 27 ms)；**新优先级 cycle 21 = dispatch_wait 优化**（最大单点 ROI），目标 1.8× 需 wall ≤ 4.22 ms | [real_workload_recal](./2026-05-14_1018_real_workload_recalibration_super_kernel_at_par.md) |
| 2026-05-14 09:45 | **Cycle 16 design plan — scatter_native + all-8-srcs-ready 三阶段 rollout** ⚠️ **基于错误 workload，ROI 修正 27→1.2 ms，见 10:18 note** | parallel-path 实现 (`MOE_USE_EXPERT_PACKED_LAYOUT` flag) 不破坏旧 path；新 IPC 字段 `expert_packed_tokens / expert_pack_pos / expert_done_count / expert_total`；Stage A 已落地 (Stage A1 0 回归 + Stage A2 +4% 双写 overhead)；Stage B 实测 packed_fc1 vs old 几乎无差距（含 overhead）。设计本身仍然有效 1.2 ms 增益，但不再是首选 | [cycle16_design](./2026-05-14_0945_cycle16_design_plan_per_expert_batched_gemm.md) |
| 2026-05-14 09:35 | ⚠️ **作废** — GEMM 22× gap 真因诊断（错误 workload 下推算的"per-(src,e) M=32 浪费 87.5%"在 `--tokens 8192` bench 里其实是 M=256 完美贴合 M_TILE，无浪费） | 见 10:18 校准 note；该假设的 *理论* 依然对（真实 DSV3 per-(src,e) M=32 确实存在 87.5% MFMA 浪费），但绝对收益从 27 ms 修正为 ~1.2 ms wall | [gemm_22x_root_cause](./2026-05-14_0935_gemm_22x_gap_root_cause.md) |
| 2026-04-09 | TileFlow tile-level overlap 调研（Comet 移植到 AMD） | stream-based overlap PASS；rocSHMEM tile path 受 ROCm bitcode 阻塞 | [tileflow_mi355x](./2026-04-09_tileflow_mi355x_tile_overlap_analysis.md) |
| 2026-04-10 | **MFMA Grouped GEMM v4c → production** | FC1 247 → 497T (+101%)，**vs CK +32% / +46%** | [mfma_gemm](./2026-04-10-mfma-gemm-optimization.md) |
| 2026-04-10 | MonolithMoE Layout C 重构 + Pack phase 优化 | OPT-1~8 理论 ~10× pack 加速，端到端预估 4.83 ms (1.75× vs baseline 8.47 ms) | [monolithmoe_layout_c_pack](./2026-04-10_monolithmoe_layout_c_pack_optimization.md) |
| 2026-04-13 | MoE comm+compute overlap 总结 | RCCL multi-stream 4× 反慢；rocSHMEM bitcode 阻塞；HIP IPC + Triton sys atomic hang | [moe_comm_overlap](./2026-04-13_moe_comm_overlap_analysis.md) |
| 2026-04-14 | IPC buffer layout (A–E) 全面对比 | Layout C 胜出 | [data_layout](./2026-04-14_data_layout_analysis.md) |
| 2026-04-14 | RCCL 无法 overlap 根因（架构层面） | host-initiated + collective semantics 决定 | [rccl_overlap](./2026-04-14_rccl_overlap_analysis.md) |
| 2026-04-14 | Tile-level overlap 数学可行性分析 | overlap 收益条件 + 临界点 | [tile_overlap](./2026-04-14_tile_overlap_analysis.md) |
| 2026-04-14 | MoE E2E baseline + overlap 上限估算 | Small 1.29× / DSV3 推理 1.13× / DSV3 训练 1.08×（70% overlap 假设） | [moe_e2e](./2026-04-14_moe_e2e_performance_benchmark.md) |
| 2026-05-08 | **CK Grouped GEMM 实现深度解析（vs v8a 530T）** | 拆解 6 项叠乘优化（Stream-K / 3-stage prefetch / LDS swizzle / 精确 waitcnt / C-shuffle / persistent counter），估算 530T → 860T 闭合路线 | [ck_deep_dive](./2026-05-08_ck_implementation_deep_dive.md) |
| 2026-05-09 → 05-12 | **P0 全程：super-kernel 24.0 → 8.80 ms（项目里最大一跃）** | 四步串联：`wgs/CU=1 ratio=0.25` (24.0→15.21) → per-src wait_flag (→14.86) → per-src compute-barrier coalescing (→14.04) → **flat tile dispatch + adaptive small-tile (→8.80)**；compute_barrier_1 8.55 → 0.29 ms；FC1+FC2 11.19 → 6.11 ms (−45 %) | [p0_full_arc](./2026-05-12_1230_super_kernel_p0_full_arc_24_to_8p8ms.md) |
| 2026-05-12 13:10 | **P1 tail-pipelining 失败 + critical-path 修正** | per-pair round-robin DSV3 8.80 → 9.98 ms regress；hybrid 持平；确认 wall = max(per-WG kernel_total) = compute，tail 不在临界路径 | [p1_tail_failed](./2026-05-12_1310_tail_pipelining_p1_failed_critical_path_correction.md) |
| 2026-05-12 13:35 | **P2 Batched (e, src, mi, ni) compute + 单相 FC1/FC2** | 24 cross-WG barrier → 3；DSV3 **8.80 → 7.95 ms（1.07× vs PyTorch+RCCL，首次反超）**；TILE-FIT **5.07 → 4.31 ms（−15 %）**；L2 weight reuse 假设证伪，真实收益来自 barrier 合并 | [p2_batched_compute](./2026-05-12_1335_batched_compute_p2_single_phase.md) |
| 2026-05-12 14:55 | **A1 per-expert pipelined compute 失败** | 把外层换成 `for e in epg` + per-expert 3 barrier；DSV3 SPARSE 7.95 → 21.6 ms (+172 %)，TILE-FIT 4.31 → 4.56 ms；并行度从 192-wide 摊到 ~30-wide + 96 vs 3 barrier；已 revert 到 P2 (`f50bb43`)；复盘 P1/A1 同款诊断陷阱 | [a1_pipelined_failed](./2026-05-12_1455_per_expert_pipelined_a1_failed_lost_flat_tile_parallelism.md) |
| 2026-05-12 15:30 | **A2 M-concat tile dispatch 失败** | 物理 gather + per-expert (e, mi, ni) flat tile + 4 barrier；DSV3 SPARSE 7.95 → 9.03 ms (+13.6 %)，TILE-FIT 4.31 → 6.75 ms (+56.6 %)；tile 数从 8192 → 1024（DSV3）/ 512 → 128（TILE-FIT < 192 WGs）→ WG 利用率坍塌；L2 reuse 假设在 P2 时已经成立（隐式），A2 是浪费；已 revert；P1/A1/A2 三连同根：MI355X compute 是 parallelism-bound，不要再动 compute 排布 | [a2_concat_failed](./2026-05-12_1530_m_concat_a2_failed_parallelism_collapse.md) |
| 2026-05-12 16:35 | **P0a 3-stage prefetch 失败 — workload 不是 HBM-latency-bound** | CK-style 3 LDS buffer + 提前 2 tile issue + 精确 `vmcnt(N)`；50/50 PASS、DSV3 / TILE-FIT 全 ratio 与 P2 在 ±1% 噪声内等价；hipcc `-Rpass-analysis` 显示 P2 已经 256 VGPR + 240 B/lane scratch + 1 wave/SIMD，3-stage 同资源；1-stage prefetch 已把 HBM 完全盖住，gap 不在这；patch 存档 `benchmarks/results/3stage_prefetch.patch`，已 revert；下一步先 profile MFMA pipeline 再决定 swizzle / waitcnt / pre-permute | [p0a_3stage_failed](./2026-05-12_1635_3stage_prefetch_p0a_failed_not_hbm_latency_bound.md) |
| 2026-05-12 17:15 | **`mfma_gemm_tile_t` 内部 profile：LDS 端是真瓶颈** | `MOE_PROFILE` build 加 6 个 GEMM bucket（gemm_total / hbm_issue / wait_vm / lds_write / sync / mfma_inner），thread-0 `clock64` + 函数尾部一次 atomicAdd（release build 零开销，VGPR/AGPR/scratch 与 P2 完全一致）；DSV3 SPARSE + TILE-FIT 两 shape 同模式：**HBM wait 1 % / sync 1 % / hbm_issue 10 % / lds_write 33–34 % / mfma_inner 43–44 %**；ASM 揭示 lds_write 实际混合了 compiler 自带的隐式 prefetch（解释 P0a 失败）+ FC2 SwiGLU silu/rcp 计算；新 P0 排序：**LDS XOR swizzle 同时打 ds_write + ds_read（−5~10 % GEMM）**，再叠 **mxfp8 weights（−2~3 ms wall）** | [gemm_internal_profile](./2026-05-12_1715_mfma_gemm_tile_internal_profile_lds_write_44pct_mfma.md) |
| 2026-05-12 22:30 | **LDS PAD 4→8 解锁 +25% Grouped GEMM** | row stride 68→72 dwords，b128 16-byte 对齐成立；Grouped GEMM 170→212T (+25%)；super-kernel DSV3 SPARSE 没动（已被 P2 锁死），但 TILE-FIT 5.07→4.31 ms 同步落地（PAD=8 是必要条件） | [lds_pad_8](./2026-05-12_2230_lds_pad_8_unlocks_25pct_grouped_gemm.md) |
| 2026-05-13 01:30 | **K_TILE 64→128 标准化** | 8 K-step / tile → 4 K-step / tile，halve wait_vm；Grouped GEMM 212→199T@K=7168（同步轻微下降，因 wait_vm 不是 bottleneck）；super-kernel DSV3 421T@K=7168 单点 micro-bench 不动；进 commit `1e6dfe8` | — |
| 2026-05-13 11:20 | **PAD8 + K_TILE128 真实 8-GPU 端到端实测** | 8× MI355X release-build wall：**DSV3 SPARSE 7.95→6.59 ms / 363→438T / 1.07×→1.28× vs RCCL**（−17 % wall）；TILE-FIT 4.31→3.79 ms / 191→217T；之前的 micro-bench 估计严重低估了 PAD=8+K_TILE=128 在端到端 wall 上的实际收益 | `benchmarks/results/dsv3_sparse_8gpu_e2e_2026-05-13.txt` |
| 2026-05-12 21:30 | **DirectToLds + K-对齐 template dispatch 落地 grouped_gemm.hip（+204%）** | `buffer_load_dwordx4_lds` builtin 把 HBM→VGPR→LDS 三步压成一条 DMA 指令；trade-off：PAD=8 → 0、persistent OOB stale data → kKAlignedKT template + `__host__ __device__` 解 SFINAE。Grouped GEMM DSV3 GateUP **199 → 643 T（+222%）/ Down 213 → 558T（+162%）**，**vs CK 升到 0.60×**。**super-kernel 移植反退 12%**——经 PAD=0-only 隔离实验最终锁定真因：**PAD=0 触发 32-way bank conflict 把 ds_read latency 推到 ~100+ cyc，DSV3 SPARSE 走的 small tile 路径只有 1 MFMA / K-step (~32 cyc) hide 不住，mfma_inner 单独 588→2470 us（即使不动 DTOLDS）。DTOLDS 这一改动本身没问题（lds_write 单独看 −40%），被 PAD=0 prereq 绑架**。Density-bump 路径（M=N=256 K=64 PAD=8）也证伪：默认 tile GEMM −40% 但 DSV3 SPARSE wall −11%（small tile 不走默认 tile + K-tile 数 2×）。**P0 重新对齐到 LDS XOR swizzle**——唯一同时帮 small tile 和默认 tile 的招数，预期 DSV3 SPARSE 6.83→3.2 ms (−54%) | [dtolds_landed](./2026-05-12_2130_dtolds_landed_grouped_gemm_plus204pct_super_kernel_reverted.md) |
| 2026-05-13 12:00 | **Phase 1：super-kernel PAD=0 + XOR swizzle 落地** | LDS_PAD 8→0（省 ~16 KB LDS），同时在 ds_write_b128 写端 (`ao/bo`) 和 lds_frag_ds<> 读端用 3-bit XOR `va ^ (ra & 7)` 对齐，stride 256 B / 2× bank period 下 32 lanes ds_read_b128 命中 8 个不同 bank quartet → 4 sub-cycles = conflict-free，与 PAD=8 基线持平。是 Phase 2 DTOLDS 的物理布局前置条件。DSV3 SPARSE 6.59→6.535 ms / 442T / 1.30× vs RCCL；correctness PASS | [dtolds_landed_xor_swizzle](./2026-05-13_1230_super_kernel_dtolds_landed_xor_swizzle.md) |
| 2026-05-13 12:30 | **Phase 2：DTOLDS 替换 ds_write_b128（FC1 全 + FC2 B）** | `buffer_load_dwordx4_lds` 把 HBM→VGPR→LDS 压成一条 DMA。FC1 (`mfma_gemm_tile_t`) 全 DTOLDS，HBM gather 用 `va_load = va ^ (ra & 7)` 反 swizzle、ao/bo 回到 natural，两条 write 路径产生**完全相同**的 swizzled 物理布局。FC2 (`mfma_gemm_tile_swiglu_t`) B 用 DTOLDS、A 保留 legacy + swizzle（SwiGLU silu(gate)*up 需要 VGPR 中转）。OOB protection: voff=0x80000000 + per-tile `rb < valid_n` check（**bug fix**：之前用了 `n_offset + rb < valid_n` 在 n_offset>0 的 tile 全 OOB）。DSV3 SPARSE 6.535→**6.268 ms / 460T / 1.35× vs RCCL**；profile：lds_write 2054→1419 us (−635 us) / hbm_issue 702→1102 us (+400 us) / mfma_inner 631→589 us / gemm_total −302 us | [dtolds_landed_xor_swizzle](./2026-05-13_1230_super_kernel_dtolds_landed_xor_swizzle.md) |
| 2026-05-13 12:45 | **Phase 2.1：SwiGLU pre-compute → FC2 全 DTOLDS** | 在 FC1→FC2 barrier 之间插一个新的 `swiglu_precompute_phase`：把 `silu(gate)*up` 直接算在 HBM 里、原地覆盖到 `fc1_scratch[t][0..F)` 的 gate 半区，up 半区不动（row stride 仍是 2*F 所以 FC1 写入零拷贝兼容）。FC2 由此切换到 plain `mfma_gemm_tile`（K_full=F, a_stride=2F），A 也走 DTOLDS。代价是 +1 个 cross-WG barrier、+94 us pre-compute、+41 us barrier；收益是 **FC2 swiglu_tiles 2612→940 us (−1672 us / −64%)**、**gemm-internal lds_write 1419→0 us（全消）**。DSV3 SPARSE 6.268→**4.825 ms / 598T / 1.755× vs RCCL** (−23% / +30% TFLOPS)；TILE-FIT 3.86→**3.39 ms / 243T** (−12% / +14%)；correctness PASS。最优 comm_ratio 从 0.18 漂移到 0.25（compute 变快后允许更多 CU 给 scatter） | [swiglu_precompute_fc2_full_dtolds](./2026-05-13_1245_swiglu_precompute_fc2_full_dtolds.md) |
| 2026-05-13 19:30 | **MMOE × Primus / DSV3 4-layer 真实训练 e2e 跑通（20 iter）** | 方案 B 接 Primus，patch `before_train` 阶段替换 `MoELayer`→`MonolithMoELayer`。一次性定位 4 个静默 crash：(1) `super()` MRO 被 module-name 重绑破坏 → infinite recursion，修：只 patch `moe_module_specs.MoELayer` 不动 `moe_layer.MoELayer`；(2) `TEColumnParallelGroupedLinear.weight0/.weight1/...` 不是 fused `weight`，修：`torch.stack([weight{i}])` 得到 `[E,2F,H]` 已是 kernel 布局；(3) HIP last-error slot 被 `hipErrorPeerAccessAlreadyEnabled` 污染 → 下次 `torch.zeros` 误报，修：`hipDeviceEnablePeerAccess` 前后 `(void)hipGetLastError()` drain；(4) eager bwd 用 `dist.all_to_all_single` 不可微 → `grad_fn=None`，修：`torch.distributed.nn.all_to_all_single` + `index_add` 替代 `index_copy_`。`MMOE_BACKWARD=eager`、`train_iters=20`、PP=1/EP=8/TP=1、mock_data 跑完，NaN check 绿、VRAM 167 GB/rank | [primus_dsv3_e2e_bringup](./2026-05-13_1930_primus_dsv3_e2e_bringup_four_silent_crashes.md) |
| 2026-05-13 21:50 | **MMOE vs baseline TEGroupedMLP loss parity 验证 (20 iter)** | 同 yaml / 同 seed / mock data 跑两次（`PRIMUS_USE_MMOE=false / true`），用新增 `MMOE_DEBUG_LOSS_TO_STDERR=1` mirror patch (priority=10) 把 `print_rank_last` 镜像到 stderr 拿到每 iter loss。**iter 1 abs diff = 1.7e-4 (≈ 1 ULP bf16)**；20 iter 全程 lm_loss max rel diff **1.22e-3 @ iter 10**，grad_norm max abs diff **3e-3**；loss 12.011→0.568 单调一致下降；无 NaN/skip。证明 **forward + eager_bwd 数值正确**。性能上 baseline 184 vs MMOE eager_bwd 69 TFLOP/s/GPU (2.7× slower)，eager 重跑了整 forward，符合预期 → 直接进 P0 `_backward_decomposed`。**附带修复**：(a) yaml `${PRIMUS_USE_MMOE:true}` 展开成字符串 `"false"` 被 `bool()` 当 truthy 的 silent 路径，patch 里加 `_coerce_bool()`；(b) Megatron 默认 `log_interval=100` 在 20 iter 跑里从不触发，yaml 显式 `log_interval=1` | [loss_parity_baseline_vs_mmoe](./2026-05-13_2150_primus_dsv3_loss_parity_baseline_vs_mmoe.md) |
| 2026-05-13 22:20 | **`_backward_decomposed` 落地：eager 69 → decomposed 107 TFLOP/s/GPU (+55%)** | 复用 forward 时 super-kernel 保存的 `permuted_input` / `fc1_gate_up_save` / `pack_perm` / `pack_offsets` / `dispatch_expert_offsets` / `dispatch_topk_weights`，手算 per-expert FC2/SwiGLU/FC1 bwd + 3 个 `dist.all_to_all_single`（combine bwd / dispatch bwd / grad-pr bwd）。**关键发现**：super-kernel sender-side sort 是 counting-sort-by-expert-id + atomicAdd 写 pack_perm，同 expert 内 *非确定性*，Python 端任何重做 argsort 都会跟 receiver permuted_input 错位 → 必须直接读 `ws.pack_perm`。三方对比 20 iter：decomposed vs baseline max abs lm_loss diff **1.85e-3 @ iter 5**, vs eager 1.80e-3；grad_norm 5.97 在 iter 10 三路完全一致；wall 615 ms → **400 ms / iter (vs baseline 232)**。后续 P0：batched bmm + fused SwiGLU bwd 估计能拉到 ~150 TFLOP/s/GPU；终局是 fused HIP decomposed bwd kernel → backward ≤ 1× forward wall | [decomposed_backward_landed](./2026-05-13_2220_decomposed_backward_landed_plus55pct_vs_eager.md) |
| 2026-05-13 23:00 | **MMOE forward 慢的真凶定位：super-kernel 本身占 95%** | 加 `MMOE_FWD_PROFILE=1` per-step timer，steady-state（iter≥3，3 layer × 10 iter）：route 0.77 / ensure_workspace 0.013 / ensure_weights 0.07（version cache 已 take effect）/ routing_to_topk 0.11 / shared_experts 0.40 / **super_kernel 26.5** / reshape 0.07 ms。MMOE 与 baseline forward-compute 的 56 ms 差距 = (26.5 − 7.8) × 3 ≈ 56 ms 全部在 kernel 自己。下一步：在训练实际 token 数下重做 bench + tile sweep | [forward_profile_super_kernel_is_95pct](./2026-05-13_2300_mmoe_forward_profile_super_kernel_is_95pct.md) |
| 2026-05-13 23:40 | **Apples-to-apples 重测：super-kernel 只在 512 t/g 工况赢** | 重编 bench（kernel struct 加了 `fc1_gate_up_save` 后 size 改了，旧 binary 必须 rebuild），sweep `T_src ∈ {512, 2048, 8192}` × `comm_ratio ∈ {0.18, 0.225, 0.25, 0.30}` × top_k=8。结果：super-kernel `4.80 → 16.98 → 64.47 ms`，PyTorch+RCCL（per-expert `torch.matmul` + RCCL all-to-all）`7.00 → 9.05 → 18.64 ms`。**512 t/g super-kernel 1.46× faster ✓，2048 t/g 1.88× SLOWER ❌，8192 t/g（生产）3.46× SLOWER ❌**。最优 `comm_ratio` 从 0.25 漂到 0.18，原本所有 P0/P1/P2 优化都是在 512 t/g 调的，到训练工况全反转。**training overhead 还在 super-kernel 单跑之上再加 56%（16.98→26.5 ms / call）**。下一步：训练工况下重做 tile / MFMA / K_TILE / comm_ratio 全 sweep，目标至少追平 PyTorch+RCCL | [apples_to_apples_loses_at_training_scale](./2026-05-13_2340_apples_to_apples_super_kernel_loses_at_training_scale.md) |
| 2026-05-14 00:05 | **Cycle 2: GEMM tile sweep — 256×256×64 winner (+10 % @ 8192)** | 训练工况 (`T_src ∈ {2048, 8192}, top_k=8`) sweep `(M_TILE, N_TILE, K_TILE)` 共 8 候选；256×256×64 / 0.25 ratio 胜出：`T_src=2048` 17.79→14.44 ms (+5.8 %)，`T_src=8192` 67.19→56.10 ms (+9.3 %)；其中 `256×256×32` 和 `64×256×128` 编译失败（LDS budget / ROWS_PER_LD_PHASE 约束） | [cycle2_tile_sweep](./2026-05-14_0005_cycle2_tile_sweep_256x256x64_wins_plus10pct.md) |
| 2026-05-14 00:18 | **Cycle 3: comm_ratio 0.250 winner（divisibility-dominated）** | 在 256×256×64 上 sweep `comm_ratio ∈ {0.125, 0.156, …, 0.5}`，0.250 胜出（`compute_wgs=192=6×32`，能被 `num_experts=32` 整除）；T_src=2048 14.44→14.32 ms (+0.8 %)、T_src=8192 56.10→55.67 ms (+0.8 %)；非整除 ratio 上性能反弹（compute_wgs 必须是 num_experts 倍数才能 stall-free） | [cycle3_commratio](./2026-05-14_0018_cycle3_commratio_0p250_divisibility_winner.md) |
| 2026-05-14 00:46 | **Cycle 4/5/5b: tile shape exploration — 256×256×64 confirmed optimal** | K_TILE constraint `K%MFMA_K==0` & `M_TILE,N_TILE % ROWS_PER_LD_PHASE(=2048/K)==0` 只允许 K∈{32,64,128,256}（256x256 tile 上）；asymmetric tiles (320×192, 224×288) 触发 LDS ~160KB envelope 或 wave layout 假设，全部 worse 或 runtime error | [cycle4_5](./2026-05-14_0046_cycle4_5_tile_shape_closed_256x256x64_optimal.md) |
| 2026-05-14 00:48 | **Cycle 6: re-profile at 256×256×64 / 0.250 — dispatch_wait now dominates** | profile breakdown @ T_src=8192：dispatch_src_ready_wait 17.5 ms (30.8 %) / fc1 16.8 ms (29.6 %) / fc2 10.9 ms (19.1 %) / copy 8.3 ms (14.6 %)；GEMM hbm 35.6 % / mfma 41.4 %（compute-bound）；copy 8.3 ms vs 理论 xGMI 2.7 ms → 3× slower → cycle 8 target | [cycle6_reprofile](./2026-05-14_0048_cycle6_reprofile_dispatch_wait_dominates.md) |
| 2026-05-14 01:00 | **Cycle 7: chunked-FC1 — implemented but NEGATIVE in single-node bench** | 期望：overlap FC1 与 dispatch_wait（按 expert batches 处理而非等所有 8 srcs 就绪）；实测：单节点 bench 中 chunked-FC1 比 monolithic 慢 30+ %（85 % slower for batch=1，B={2,4,8,16,32} 都 worse），因为单节点没有真实 network/compute skew 可 hide；保留 `MOE_CHUNKED_FC1` 编译标志为多节点用 | [cycle7_chunked_fc1](./2026-05-14_0100_cycle7_chunked_fc1_negative_result.md) |
| 2026-05-14 01:30 | **Cycle 8: parallel-peer copy_to_combine — -9 % wall both shapes** | `copy_to_combine` 原先 `for (src 0..7)` 顺序写 8 个 peer，单瞬只用 1 条 xGMI 链；改为 `my_src = wg_local_id % NUM_GPUS` 把 128 个 compute WG 分成 8 组并发写 8 个 peer，所有 7 条 outbound xGMI 同时活跃。实测 copy 8.30 → **2.88 ms (-65 %)**，xGMI util 32 → **93 %**；wall T_src=2048 14.43→13.09 ms (882 TFLOPS / +13.6 %)、T_src=8192 56.08→**50.87 ms (908 TFLOPS / +10.3 %)**。同样的 trick 用在 scatter 上反而 +51 %（receiver-side HBM bank contention + atomic 序列化），revert | [cycle8_parallel_peer_copy](./2026-05-14_0130_cycle8_parallel_peer_copy_minus10pct.md) |
| 2026-05-14 01:55 | **Cycle 11: dispatch_wait split instrumentation** | bucket 20/21 拆分 dispatch_src_ready_wait → until_first_src + first→all (skew)。结果（T_src=8192）：total 17.94 ms = until_first 16.85 ms (94 %) + skew 1.10 ms (6 %)。Host `launch_all` 仅 114 µs，per-rank hipEvent spread 仅 0.13 ms —— **不是 launch skew**；最可能是 comm WG 和 compute WG 处于不同 SE clock domain，profile cycle-to-us 换算对 comm WG 不准；wall（latency_ms / hipEvent）才是 ground truth | [cycle11_dispatch_wait_skew](./2026-05-14_0155_cycle11_dispatch_wait_launch_skew.md) |
| 2026-05-14 02:30 | **Session 总结：bench 进展 8192 67→50.87 ms (-24 %)，仍 2.73× slower vs PyTorch+RCCL** | 今天三轮优化：Cycle 2 tile (-10 %) + Cycle 3 ratio (-1 %) + Cycle 8 copy (-9 %) ≈ -24 % cumulative。剩余 wall：dispatch_wait 35 % (clock-domain artifact, 实际应 <10 ms in training) / FC1 34 % (62 % of peak, hipBLASLt-class gap) / FC2 22 % / copy 6 % (基本 saturate) / swiglu 2 % / barrier 1 %。1.8× 目标需 GEMM 达 80 %+ peak → 现阶段 stretch；下一 cycle 优先：(1) 跑真实训练验证 dispatch_wait 是否真的训练里消失，(2) wave layout `<WLM, WLN>` sweep，(3) GEMM prologue/epilogue 剩余 20 % unattributed 拆分 | [session_progress_cycle8](./2026-05-14_0230_session_progress_cycle8_landed_diagnostics.md) |
| 2026-05-14 02:40 | **Cycle 13: wave-layout sweep — 2×2 confirmed optimal** | 通过 `MOE_WAVE_LAYOUT={22, 14, 41}` 编译开关 sweep 默认 tile (256x256x64) 的 wave-block layout：`<2,2>` 13.07 / 50.73 ms；`<1,4>` 13.35 / 51.31 ms (-2 %)；`<4,1>` 13.05 / 50.71 ms (噪声内 tied)。`<2,2>` 平衡 A/B 端 L1/L2 reuse 是 sweet spot；`<1,4>` (寛 N, 窄 A 内 reuse) 输 2 %；`<4,1>` (寛 A, 窄 B 内 reuse) 数值持平但代码改动更多，保 default `<2,2>`。`MOE_WAVE_LAYOUT` flag 保留为后续 grouped-GEMM / per-tile-class dispatch 备用 | — |
| 2026-05-14 09:16 | **T_src dispatch 基础设施落地（Generic / Large 双 super-kernel binary）** | 引入 `MoeKernelVariant` enum (`Generic` / `Large`，阈值 `MOE_LARGE_T_SRC_THRESHOLD=4096`)，`expert_compute_phase` template 化、entry kernel 拆分为 `__device__` body + 2 个 `extern "C" __global__` wrapper；launcher 按 `args.tokens_per_gpu` 分派并打 `[moe-super-kernel] variant=...` 一行 stderr trace；occupancy query 各自 cache。同步落地第 1 个 Large 特化：4 个 `MOE_HAS_SMALL_TILE` 站点全包 `if constexpr (V != Large) { … continue; }`，让 Large 变体彻底 DCE 小 tile 路径。基础设施 0 回归（Generic T_src=2048 13.13 ms vs cycle8 13.09，Large T_src=8192 50.29 ms vs cycle8 50.87，均在跑间噪声内），DCE 单独 0 收益 —— 后续所有 8192 专属优化都在 Large 内部 `if constexpr` 落地，不再污染 Generic 路径 | [t_src_dispatch_infra_landed](./2026-05-14_0916_t_src_dispatch_infra_landed.md) |

## 下一步（按 ROI）

| 优先级 | 方向 | 说明 |
|---|---|---|
| ~~**P0** XOR swizzle (legacy only)~~ | ~~只砍 mfma_inner bank conflict~~ | **2026-05-13 落地为 Phase 1**：实测 wall 6.59→6.535 ms (持平), 关键是 enabled Phase 2 的物理布局前置 |
| ~~P0+ wave-block layout~~ | ~~DTOLDS + XOR-equivalent 同时存在~~ | **2026-05-13 落地为 Phase 2**：DTOLDS 线性写 + HBM gather 反 swizzle 等价于 XOR swizzle 的写端，物理布局完全一致。wall 6.59→6.268 ms (−5%) |
| ~~**P0** Phase 2.1：FC2 SwiGLU 路径 DTOLDS~~ | ~~预期 wall 6.268→~5 ms (−20%) / 1.69× vs RCCL~~ | **2026-05-13 落地**：实测 wall 6.268→**4.825 ms (−23%) / 598T / 1.755× vs RCCL**，超过预期 |
| **P0** | **decomposed bwd 优化（107 → 150+ TFLOP/s/GPU）** | (a) per-expert loop → batched `torch.bmm` 或 grouped-GEMM；(b) fused SwiGLU bwd HIP kernel (gate*sig*(1-sig) elementwise 合并)；(c) per-(src, expert) gather 改成 single HIP permute kernel 省 cat 开销；(d) `out_recv_e` 在 forward 末尾留下来避免 bwd 重算。终局是把整个 bwd 写成 fused super-kernel → backward ≤ 1× forward wall。预期可把 MMOE 总训练 wall 拉到 baseline 之下 |
| **P0** | **tail-wave critical-path 修正** | gather_combine_phase 7.93 ms / iter，已接近 compute 2 倍。compute 缩到 4.8 ms 后 tail 的 atomic-combine HBM 端 latency 可能反扑（如果 IPC 同步 ~5 ms）。需 (a) restructure atomic_add 调用频率 / batching 或 (b) 把部分 combine 工作移到 scatter 侧。先 profile 8-GPU tail wall 是否真在 critical path 上 |
| **P0** | **dispatch_src_ready_wait 重叠** | 当前 1503 us 纯等 IPC scatter 落地；可考虑让 FC1 prologue（pair descriptor 构建、weights L2 prefetch）和 wait 并行 |
| P1 | **per-tile-class K_TILE template**（default tile 用 M=N=256 K=64，small tile 保持 K=128） | Step 1 实验已验证默认 tile GEMM 内部 −40%；real win for TILE_FIT-like workloads。LDS region union + kK/kPad 模板参数；DSV3 SPARSE 不动；1-2 d |
| **P0** | **FP8 / mxfp8 weights for FC1 / FC2** | A2 失败后唯一剩下的 DSV3 大杠杆：HBM weight 流量直接 ÷2，与 layout 重排正交（不影响 tile 数 / WG 利用率），与 XOR swizzle 也正交。MI355X 原生支持 mxfp8 MFMA。预期 DSV3 FC 5.86 → ~3 ms（−2.8 ms） |
| **P0** | **TILE-FIT scatter 总时间下降**（pack+scatter 合并 / per-rank XGMI link 并行 / skip empty pair early） | A1 / A2 证伪「让 wait/L2 reuse 帮忙」的路径；要省 wall 必须真正缩短 scatter 的物理时间，目标 TILE-FIT −1 ms |
| P1 | Weight pre-permutation to MFMA fragment layout | 把 weight 离线重排成 MFMA fragment 序，去掉 LDS write + 减少 ds_read 仲裁；与 FP8 正交 |
| P1 | C-shuffle epilogue | +3–5 %，FC2 影响小，可与 FP8 联动 |
| P2 | Work-stealing tile counter（atomic 替代 round-robin） | 吸收 T_e 不均，−0.2~0.5 ms |
| P2 | ~~K-step 间去掉 `lgkmcnt(0)` 隐式 barrier~~ | profile 证伪：`sync_per_ktile` 占 1.1 %，没收益（[gemm_internal_profile](./2026-05-12_1715_mfma_gemm_tile_internal_profile_lds_write_44pct_mfma.md)） |
| P2 | ~~3-stage prefetch~~ | 已证伪（[p0a_3stage_failed](./2026-05-12_1635_3stage_prefetch_p0a_failed_not_hbm_latency_bound.md)）：workload 不吃 HBM 延迟隐藏；GEMM profile 又复证 `wait_vm` 仅 1 % |
| P3 | 重启 tail WG 并行化（compute 真比 tail 快之后） | 必须先把 atomic spin 换成 LDS-cached flag，否则像 P1 一样回退 |
| P3 | 跨节点 ROCShmem + Persistent Kernel | IB 场景下 comm 更长，overlap 收益更大 |

## 文件索引

| 主题 | 文件 |
|---|---|
| GEMM kernel | [`2026-04-10-mfma-gemm-optimization.md`](./2026-04-10-mfma-gemm-optimization.md), [`2026-05-08_ck_implementation_deep_dive.md`](./2026-05-08_ck_implementation_deep_dive.md) |
| Super-kernel + Layout | [`2026-04-10_monolithmoe_layout_c_pack_optimization.md`](./2026-04-10_monolithmoe_layout_c_pack_optimization.md), [`2026-04-14_data_layout_analysis.md`](./2026-04-14_data_layout_analysis.md) |
| Overlap 调研 / 排障 | [`2026-04-09_tileflow_mi355x_tile_overlap_analysis.md`](./2026-04-09_tileflow_mi355x_tile_overlap_analysis.md), [`2026-04-13_moe_comm_overlap_analysis.md`](./2026-04-13_moe_comm_overlap_analysis.md), [`2026-04-14_rccl_overlap_analysis.md`](./2026-04-14_rccl_overlap_analysis.md) |
| 可行性 + baseline | [`2026-04-14_tile_overlap_analysis.md`](./2026-04-14_tile_overlap_analysis.md), [`2026-04-14_moe_e2e_performance_benchmark.md`](./2026-04-14_moe_e2e_performance_benchmark.md), [`2026-05-14_1210_operator_forward_breakdown_us_8466.md`](./2026-05-14_1210_operator_forward_breakdown_us_8466.md) |
| Super-kernel 调优 (E2E) | [`2026-05-12_1230_super_kernel_p0_full_arc_24_to_8p8ms.md`](./2026-05-12_1230_super_kernel_p0_full_arc_24_to_8p8ms.md), [`2026-05-12_1310_tail_pipelining_p1_failed_critical_path_correction.md`](./2026-05-12_1310_tail_pipelining_p1_failed_critical_path_correction.md), [`2026-05-12_1335_batched_compute_p2_single_phase.md`](./2026-05-12_1335_batched_compute_p2_single_phase.md), [`2026-05-12_1455_per_expert_pipelined_a1_failed_lost_flat_tile_parallelism.md`](./2026-05-12_1455_per_expert_pipelined_a1_failed_lost_flat_tile_parallelism.md), [`2026-05-12_1530_m_concat_a2_failed_parallelism_collapse.md`](./2026-05-12_1530_m_concat_a2_failed_parallelism_collapse.md), [`2026-05-12_1635_3stage_prefetch_p0a_failed_not_hbm_latency_bound.md`](./2026-05-12_1635_3stage_prefetch_p0a_failed_not_hbm_latency_bound.md), [`2026-05-12_1715_mfma_gemm_tile_internal_profile_lds_write_44pct_mfma.md`](./2026-05-12_1715_mfma_gemm_tile_internal_profile_lds_write_44pct_mfma.md), [`2026-05-12_2230_lds_pad_8_unlocks_25pct_grouped_gemm.md`](./2026-05-12_2230_lds_pad_8_unlocks_25pct_grouped_gemm.md), [`2026-05-12_2130_dtolds_landed_grouped_gemm_plus204pct_super_kernel_reverted.md`](./2026-05-12_2130_dtolds_landed_grouped_gemm_plus204pct_super_kernel_reverted.md), [`2026-05-13_1230_super_kernel_dtolds_landed_xor_swizzle.md`](./2026-05-13_1230_super_kernel_dtolds_landed_xor_swizzle.md), [`2026-05-13_1245_swiglu_precompute_fc2_full_dtolds.md`](./2026-05-13_1245_swiglu_precompute_fc2_full_dtolds.md) |
| 训练框架接入 (Primus / Megatron) | [`2026-05-13_1930_primus_dsv3_e2e_bringup_four_silent_crashes.md`](./2026-05-13_1930_primus_dsv3_e2e_bringup_four_silent_crashes.md), [`2026-05-13_2150_primus_dsv3_loss_parity_baseline_vs_mmoe.md`](./2026-05-13_2150_primus_dsv3_loss_parity_baseline_vs_mmoe.md), [`2026-05-13_2220_decomposed_backward_landed_plus55pct_vs_eager.md`](./2026-05-13_2220_decomposed_backward_landed_plus55pct_vs_eager.md) |
| 外部文献对标 | [`2026-07-07_latest-moe-systems-papers-scan.md`](./2026-07-07_latest-moe-systems-papers-scan.md) |

> 周报视角：[`weekly-reports/2026-04-14_weekly_report_0409_0414.md`](../weekly-reports/2026-04-14_weekly_report_0409_0414.md)

## 维护约定

- **每一次优化都要写一篇 note**（即使是失败实验或临界路径修正），命名格式
  `YYYY-MM-DD_HHMM_<topic_slug>.md`，遵循 `progress-note` skill 模板。
- 每写一篇 note → 同时回写本 README 的 **进展时间线**、**状态** 和 **下一步**。
- 老 note 不改（除非加 superseded 横幅），项目当前状态以本 README 为单一真源。
