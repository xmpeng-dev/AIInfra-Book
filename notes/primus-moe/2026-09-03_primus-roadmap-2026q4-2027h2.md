# [ROADMAP] Primus 2026 Q4 – 2027 H2

> 综合四路调研产出：`slab` 调研沉淀、上游 TorchTitan、上游 Megatron-LM、Primus 当前实现。
> 前序文档：[`2026-05-29_roadmap_h2_2026.md`](./2026-05-29_roadmap_h2_2026.md)（MoE 专项，对标 `NVIDIA/Megatron-LM#4815`）。本文是全框架级别的上层规划，MoE 细项仍以前序文档为准。
> 调研基线：Primus `6e2b114b`（2026-08-30）、Megatron-LM `fb7ba4c83`（2026-09-02，core 0.20.0-dev）、TorchTitan `c4ad49b5c`（2026-09-02）、slab `cfd02bd`（2026-08-24）。

---

## 0. 执行摘要

三句话版本：

1. **Primus 最大的风险不是性能，是上游欠债。** 156 个 patch 里只有 1 个声明版本门，而 Megatron 已经落后 1391 commits（core 0.16→0.20）、TorchTitan 落后 937 commits（+168K 行）。上游同时废弃了 `GPTModel`、删除了 `megatron/legacy/`。下一次升级不是 rebase，是硬切。
2. **两个上游官方扩展点刚刚就位，这是一个时间敏感的战略窗口。** Megatron 的 `BackendSpecProvider`（2026-08-21 `881dc7605` 转正）和 TorchTitan 的 `torchtitan/overrides/` 都是为"厂商注入 kernel"设计的，而且上游自己在用。把 monkey-patch 转成受支持插件，是唯一能从结构上降低维护成本的动作。
3. **护城河已被实证，比预想的宽。** 上游 Megatron 的 `megatron/core/` 里 ROCm 相关命中 1 处、`torch.cuda.` 调用 796 处，无设备抽象层；上游 TorchTitan 直接停掉了 ROCm CI、删掉 ROCm golden loss、把 ROCm 排除在发版验证之外、并把 AMD 用户指向独立 fork。zero-bubble / DualPipe 在上游 Megatron 是 **0 命中**。

优先级排序原则：**先修可信度（度量 + 正确性 CI），再修结构（插件化 + 版本门），再收性能红利（已根因定位的存量项），最后投研究线。** 顺序反了会导致"拿噪声数据去驱动架构决策"。

---

## 1. 定位判断

### 1.1 Primus 是什么

不是"Megatron 的 AMD 封装"。按代码事实：`primus/` 有 21.4 万行 Python，其中 patch 层只占 megatron 后端的 17%，而 `primus/core/pipeline_parallel/`（8,975 行）单独就超过全部 megatron patch（7,975 行）。**Primus 里已经有约 33,000 行执行层代码在生产运行，只是它们分散在三个 backend 命名空间里，没有被当作一个执行层来管理。**

这决定了对外叙事和内部立项的措辞：不是"请批准我们造一个框架"，而是"我们已经有一个执行层，请批准把它从三个 backend 命名空间里收拢出来"。（`notes/primus-moe/2026-08-04_1018_framework-strategy-patch-layer-vs-execution-layer.md`）

### 1.2 上游取舍矩阵

| 判断 | 内容 | 依据 |
|---|---|---|
| **停手**（上游正在或已吸收） | MoE dispatch / EP 通信逻辑本身（上游已 4 条 dispatcher 路径）、1F1B+A2A overlap、细粒度重算与激活卸载、FP8 recipe 全矩阵、HF checkpoint 互操作（归 Megatron-Bridge）、inference / RL refit、Muon/SOAP/Lion 优化器、自研 FSDP（MFSDP v1 已覆盖 ZeRO-1/2/3+HSDP+EP+MXFP8） | 上游 Megatron 调研 §"about to absorb" |
| **必须追赶**（上游破坏性变更） | `GPTModel`→`HybridModel`、`parallel_state` 全局量→`ProcessGroupCollection`、TE 版本格（~25 个版本谓词）、TorchTitan TOML→Python dataclass 配置 + `spmd_types` 默认 + `ShardingConfig` + `config_registry.py` 删除（无 shim） | 两份上游调研 §"version-alignment risks" |
| **独占**（上游不会做） | ROCm 全栈持续验证、ROCm grouped GEMM（上游 `LocalSpecProvider` 明确留空）、zero-bubble / DualPipe 调度、ROCm MoE 通信（DeepEP/HybridEP 两个上游都无 ROCm 路径）、量化 collective（无人认领）、容量规划（上游删掉了 estimation 脚本）、MI300A APU 统一内存 CPU offload | 本文 §1.3 |

### 1.3 护城河的实证强度

```
上游 Megatron-LM (fb7ba4c83)
  megatron/core/ 内 rocm|torch.version.hip 命中 ......... 1
  megatron/core/ 内 torch.cuda. 调用 ................... 796
  设备抽象层 .......................................... 无
  CI 硬件 ............................................. DGX A100 / H100 / GB200
  dualpipe|zero-bubble 命中（megatron/ + docs/）......... 0

上游 TorchTitan (c4ad49b5c)
  set-matrix.yaml:86 ............... ROCM_MATRIX=""  # "ROCm remains disabled"
  ROCm golden loss 文件 ............ 已删除（2026-08-24, #4127）
  docs/release.md:407 .............. ROCm 列于 "Tests not currently run against the staged RC"
  README ........................... 将 AMD 用户指向 AMD-AGI/torchtitan-amd fork
  DeepEP / HybridEP ROCm 路径 ...... 无（#3576）
  fsdp_symm_mem on ROCm ............ skipped（#3597）
  NVFP4 ............................ SM100 only
```

这不是"上游暂时没排期"，是架构上就没打算做。**护城河可以按年规划，不必按季度防守。**

---

## 2. 现状资产盘点（优异点）

以下都是已落地、有实测数字支撑的差异化能力，可直接用于对外叙事。

### 2.1 MoE 融合执行：MegaMoE

FlyDSL 实现的融合 MoE 层，把 expert all-to-all 折进喂给它的 grouped GEMM。

- kernel 级 **1.51×**，端到端最高 **1.123×**
- **与低精度叠加增益**：MXFP8 下 1.123× > BF16 下 1.082×，因为量化其他部分后 MoE 在 step 中占比上升
- 同时降峰值显存：MXFP8 配对下 **142.4 GiB vs 150.1 GiB**，因为置换后的 token buffer 从未落地
- 加速比在故意制造的路由不均衡下不变，即不依赖专家负载均匀

（`docs/tech_blogs/primus-moe-optimization/README.md`）

### 2.2 低精度 grouped GEMM

- FP8（tensorwise）grouped GEMM 相对 BF16：前向随 M 增长 **1.2×@M=2048 → 1.6×@M=8192**，反向稳定 **1.5–1.7×**（含量化开销计入 FP8 侧）
- Turbo + FlyDSL 附加开关在已含基础优化的配置上再加 **Qwen3-235B-A22B +16.2% / GPT-OSS 20B +9.7% / Qwen3-30B-A3B +5.8%**
- 反向收益更大，而反向本身是这一层的大头

### 2.3 大规模稳定性实证

DeepSeek-V3，1024× MI355X：

- 调优后的 pipeline layout + 按规模显式 recompute ID → **1.095×** 基线吞吐
- 边界 rank 气泡 30.6% / 35.1% → **22.0% / 11.7%**
- **1700 连续步**：中位步时 22.08 s，**93.2% 的步在 ±2% 内**，10.6 小时漂移 **1.8%**
- 扩展性：固定配置下 128 节点保持 32 节点单卡吞吐的 **98.5%**（4× 卡数只损失 1.5%）；按规模重调后达 **104.1%**（更宽的 DP 组省出显存，重算从 15 层降到 9 层）

### 2.4 训练前规划：Primus Projection

- 41 个文件、15 个模块 profiler、13,394 行，含 Origami GEMM + SDPA 模拟器
- 已发布验证案例**全部落在实测 10% 以内**，MoE 案例（8 节点 Mixtral 8x22B, EP8/PP4/VPP2）**1.4%**
- 有纯 CPU 模拟模式，不需要 GPU
- 是 Primus Tuning Agent 的打分 oracle
- **上游对标物为空**：TorchTitan 只有一个 SLURM 文件，且在 #2386 里把 `scripts/estimate/estimation.py` 删了

### 2.5 流水线调度家族

31 个 scheduler 文件，覆盖 zb / zbv / zbv_greedy / v1f1b / vpp / seq1f1b / token 级 ZB / auto-schedule / offloading，另有 6 个算法的独立实现、`primuspipe/` handler 运行时、PP 可视化器、ODC（基于 ROCm/MORI + rocSHMEM 的按需通信）。

**上游 Megatron 的 zero-bubble / DualPipe 命中为 0。** 这是当前领先且短期无碰撞风险的一块。

### 2.6 JAX 路径的 dropless MoE

grouped GEMM + DeepEP 让纯 JAX 做不到的配置变得可行：

- 移除约 **444 GiB** 的 expert-matmul 墙；DeepEP 更瘦的 all-to-all 降低约 15% 瞬时占用，换回一档 batch size
- per-device batch 8–9 可行，而 `ragged_all_to_all` dropless 路径在 8 就 OOM
- 约 **1180 tokens/s/device**，在所有可行 batch size 上优于 `ragged_all_to_all`，达到 `capacity_factor=4` 稠密配置的约 **2×**

### 2.7 工程可信度资产（被低估的差异化）

- **`primus/core/config/schema_check.py`（1,200 行）**：AST 静态读上游 schema（不 import torch/jax），从 `get_param()` 调用和 dataclass 子类推导合法扩展而非维护手写清单，进 CI `code-lint`。**它的存在源于一次真实事故**——`debug_moe_force_load_balance` 被上游改名，导致"所有 DeepSeek 配置都在关着 MoE 负载均衡的情况下训练"。手写逃生舱只有 79 行。
- **DeepEP 静默错梯度根因定位**（`report/deepep-bf16-loss-investigation.md`，620 行）：DeepSeek-V3 BF16 loss 开 DeepEP 卡在 6.1 vs 关闭 4.52。推翻约 10 个假设、自建 5 个 GPU 探针，其中发现**探针自身会掩盖 bug**（backward hook 里的 host I/O 让复现率 6/6 → 0/6）。根因是 `intranode.hip:365-369` 的 `kUseCheapFence` 只做编译器 fence + per-wave `s_waitcnt` + relaxed store，其他 warp 写的数据没有 system-scope 写回、接收端读到 stale L2。验证 0/8 vs 13 坏行，loss 4.5214 vs 参考 4.5213，代价 +2.5%。已修进上游并由 `4d2f7a74`(#1048) 取回。

这两件事讲的是同一个故事：**Primus 有能力发现并根治别人发现不了的静默正确性问题。** 对企业客户，这比多 5% 吞吐更值钱。

---

## 3. Roadmap

### 阶段 C0 — 可信度地基（2026 Q4，约 6–8 周）

> 这一阶段不产出新功能。它的唯一目的是让后续所有性能与架构决策"可被验证"。跳过它，后面每个数字都是可争议的。

| ID | 条目 | 验收标准 |
|---|---|---|
| **C0-1** | **度量可判定性标准**：固定时钟（或选定展散最小的卡并锁定）、报告比值与 PMC 计数而非绝对值、30+ 次重复给出分布、记录环境指纹。同时修节点配置（`rocm-smi --setperflevel high` 当前 "Not supported"，sclk 在 level 0/1 间跳，绝对值摆动 ±30%） | 任何对外/立项数字附带分布与指纹；<10% 的结论在修复前不得引用。这同时是 Pilot BASELINE 非确定性（已阻塞 15 个月）的同一根因 |
| **C0-2** | **patch 层失败可见化**：`_get_backend_version()` 当前吞异常返回 `None`，`_match_backend_version()` 随即返回 `False` → **静默跳过**。改为 fail-loud；启动时打印"期望 vs 实际"的 patch 清单并在不匹配时退出；重复 patch id 报错；`_patch_guard` 提升进 core 并扩到 TorchTitan | 上游挪动 `package_info.py` 时训练**拒绝启动**而非静默降级。**必须在 C0-3 之前完成**——先补版本门会让一次探测失败禁掉整个 patch 层 |
| **C0-3** | **patch 层可机读化**：给 `register_patch` 增加 `targets=[...]` 声明所替换的上游符号，回填全部 156 个；CI 增加 lock-bump 影响报告（diff 新旧 `_thirdparty.lock` 之间 `targets` 符号的源码，列出需复核的 patch） | 目前 **0/156 声明替换目标**，bump 时无法做影响分析。上游按 commit 钉，风险全部集中在 bump 那一刻 |
| **C0-4** | **多机数值正确性 CI**：至少 2 节点、bf16 + fp8 各一条 loss golden，覆盖 DeepEP 开/关 | 这正是 DeepEP 静默错梯度所属的 bug 类，当前 CI 完全无覆盖。同时把 `bk/` 下的数值调试工具（`probe_rowmap.py`/`probe_graddump.py`/`cmp_moestage_ranks.py`）从 gitignore 提升为正式工具 |
| **C0-5** | **补齐被注释掉的测试**：`test_megatron_trainer.py:503` 的 zero-bubble E2E 用例恢复（对应 31 个 scheduler 文件目前 0 E2E）；MXFP4/MXFP8 补训练级 E2E | zero-bubble 是 §2.5 的差异化主张，不能没有 E2E |
| **C0-6** | **统一 Primus-Turbo 钉法**：`ci.yaml`(`6d5ff979`) 与 `benchmark.yaml`(`a04a233c`) 当前钉不同 commit，即正确性与性能验证跑的不是同一套 kernel；Turbo 在 `pyproject.toml`/`requirements.txt` 里完全未 pin，且无运行时最低版本断言 | 单一来源 + 运行时断言。顺带解决 MegaMoE 需要 Turbo `9b5d3092`+ 而"默认镜像不带"的问题——**旗舰功能在发布镜像里跑不起来** |
| **C0-7** | **对外口径修正**：README 宣称的 "reinforcement learning workflows"（全树 RL/RLHF/GRPO/PPO 实现代码为零）与 "built-in fault tolerance"（仅 requirements + 文档页，训练无集成）；恢复 backend-gap dashboard 的数据产出（5 个构建脚本与部署 workflow 都在，`docs/backend-gap/` 数据目录不存在） | 可信度问题，成本极低 |

### 阶段 C1 — 插件化与上游对齐（2026 Q4 – 2027 Q1）

> 战略窗口期。两个上游扩展点都是新的，现在介入既能少写 patch，也能影响接口形状。

| ID | 条目 | 依据与验收 |
|---|---|---|
| **C1-1** | **Primus-Turbo `BackendSpecProvider`**（最高杠杆单项投资）：实现 `column_parallel_linear` / `row_parallel_linear` / `fuse_layernorm_and_linear` / `layer_norm` / `core_attention` / `grouped_mlp_modules` / `activation_func`，提供 ROCm grouped GEMM、融合 LN+Linear、融合 attention | 上游 `LocalSpecProvider.grouped_mlp_modules(moe_use_grouped_gemm)` **接收参数却完全不读**，无条件返回 `SequentialMLP`（`backends.py:133-141`）；`InferenceSpecProvider` 返回 TE 版。即**上游把"非 TE 的 grouped GEMM"这个位置明确留空**。多数 spec builder 显式接受 `backend: BackendSpecProvider = None`，可注入无需 patch。**远优于 fork TE** |
| **C1-2** | **TorchTitan 集成迁到 `@override`**：把 21–22 个 monkey-patch 转成 `torchtitan/overrides/` 注册；Primus/Turbo 每个 kernel 注册为 PyTorch custom op（`torch.library.custom_op`/`triton_op` + `register_fake` + `register_autograd`，`opcheck` 验证） | 上游 554 行 README 动机原文即"offer vendors a clean extension point"，且自己在用（`fused_mla.py` 29KB / `fused_swiglu.py` 26KB / `helion_rope.py` 41KB / `moe_token_dispatcher.py` 对 DeepEP+HybridEP 做 `@override`）。**custom op 注册是硬前提**——TorchTitan 默认编译 transformer block，裸 kernel 调用会 graph-break。产出可作为外部包 `primus-torchtitan-rocm` 分发 |
| **C1-3** | **采纳 HybridModel**（最高 severity 升级项）：迁到 pattern DSL，用 `gpt_checkpoint_interop.py` 做载入期转换（需要 `--dist-ckpt-optim-fully-reshardable` 存的 checkpoint） | `gpt_model.py:126`：*"GPTModel IS DEPRECATED... only accepting critical bug fixes, no new features"*，0.19 起新架构只进 HybridModel。HybridModel 重定义"一层"：一个 GPT block 变两层、层索引位移、`decoder.final_layernorm`→`final_norm`、pattern DSL 取代 7 个 pipeline layout 参数。129 个 Megatron patch 中凡绑 GPT 层索引 / `gpt_layer_specs` / GPT state-dict 的全部会断 |
| **C1-4** | **后端版本推进到 `core_rX.Y.0` release 分支**：目标 `core_v0.19.0`（2026-08-19），放弃钉 main SHA；同步处理 `megatron/legacy/` 已物理删除（钉在 ~2026-04 之前是硬切非 rebase）、`parallel_state` 全局量→`ProcessGroupCollection`、Python 3.12 / torch ≥2.6 地板 | 上游 deprecate-at-N / remove-at-N+2 只给约 4 个月反应窗口（6–9 周一个 minor），**跳两个 release 很危险**。注意 API 兼容闸（`griffe`）只保护公开函数签名，类内部/私有方法/属性名/控制流零保护——恰好是 patch 绑定的东西 |
| **C1-5** | **上游扩展点 PR 2–3 个**：scheduler 注册、spec/provider 补全、collective backend 插件。前两个上游已部分存在，属"请补全你们已有的扩展点"；第三个作为"对多加速器厂商都有益"提出 | 一个季度内拿到明确信号，成本远低于先做重构。Primus 目前 **OSS PR 记录为零**；每个 merge 的 PR 都是可引用的归属影响，且顺带降债 |
| **C1-6** | **依赖方向 CI 约束**：import-linter 规则强制 `adapters → runtime → platform/Turbo` 单向，冻结现有反向依赖不再增长 | `primus/backends/transformer_engine/` 是 vendored-and-modified TE 源码，**4 个文件反向 import `megatron`**；`comm_overlap.py` 单文件同时 import `transformer_engine_torch` + `primus_turbo` + `hip` + `megatron.core.utils`，四层全穿 |
| **C1-7** | **平台能力契约**：planner 查询"SDMA 是否存在 / symmetric memory 是否存在 / XGMI 是否全连 / HIP graph 是否支持"，取代算法里散落的 `if gfx950` 与 patch 的 `condition=` 判断 | 一个 planner 同时服务 MI300X 与 MI355X 的前提 |

### 阶段 C2 — 存量性能红利（2027 Q1 – Q2）

> 全部已根因定位，属于"kernel 已经做完、包装层是 bug"类型，ROI 最高。

| ID | 条目 | 量化依据 |
|---|---|---|
| **C2-1** | **修 Primus DataLoader**（Bridge `dataset_provider` 用 spawn multiprocessing context）；并把 `fp8_param` / precision recipe 暴露到 yaml | Llama-2-70B LoRA 上 Primus vs NeMo **8.4% 步时差距的全部**是一个 191 ms 单进程 DataLoader 空转（8192² mask 构造约 125 ms 在主线程）。修完应**反超 NeMo**。另有约 70/85 GB 显存差距来自 `fp8_param=True` 运行时强制覆盖且 yaml 里不可见——本身就是可用性缺陷 |
| **C2-2** | **降 `PrimusFP8GroupedMLP` 框架开销**：简化 7 张量 autograd `apply()`、消除 `tokens_per_expert.tolist()` 的 D2H 同步、减轻 weight cache；或把 FP8 expert GEMM 并入 Megatron `GroupedMLP` 复用其精简框架 | FP8 expert-GEMM 路径端到端慢 14%（1,980→2,290 ms），但 **GEMM 只贡献 +3 ms(1%)，277 ms(99%) 是模块/框架开销** |
| **C2-3** | **SwiGLU 融进 fc1 epilogue 或 fc2 prologue** | swiglu 在四个生产模型上都跑 **3–5 TFLOPS**（带宽受限），是唯一让矩阵引擎闲置的 stage。"全线约 3% 的干净收益"。先例：SwiGLU 预计算把 FC2 swiglu_tiles **2612→940 µs(−64%)** 并消除 GEMM 内 `lds_write` |
| **C2-4** | **修 `PRIMUS_TURBO_AUTO_TUNE` 作用域**：限定到 grouped-GEMM dispatcher（或按 dispatcher opt-in）；并解决 `HAVE_DEEP_EP` / 独立 `deep_ep` 安装问题 | autotune 在 grouped GEMM 上 **+10~+31%**，但每次都让 DeepEP combine/dispatch **退化 5–14%**（复现：关 2,544 µs ±0.6% vs 开 2,887 µs ±2.2%）。根因已定位，修完从"好坏混合"变纯收益 |
| **C2-5** | **permute/unpermute Triton 化**（Turbo 已有更新实现可切），并按 `check_for_nan` 的思路审计 Megatron 剩余的 device-side reduce + CPU 同步点（动态 loss scale 调整、grad scaling、assertion） | permute/unpermute 在 B11 trace 里 **36 ms/iter**（估计 −2~−3% 步时，1–2 人日）；`check_for_nan=false` 单项就是 **−7.5% 边际**，是 GPT-OSS 战役里最大的单个配置收益 |
| **C2-6** | **拆掉 quant↔GEMM 格式契约**：让 GEMM prologue 直接消费未量化输入、/或 epilogue 直接产出下游所需 scale 格式，然后把新契约冻进 Turbo 算子接口。分级推进 S0 可判定性 → S1 契约映射 → S2 单点原型 → S3 泛化 | **FP8 GEMM 本身已有 1.84–1.96×，被 colwise requant/quant 稀释到净 1.15–1.29×**。dW1+dW2 上约 1.15 ms 量化开销，只跑到 HBM 带宽的约 35%。kernel 局部优化已穷尽（字节精确融合双量化、无 bank conflict、VALU 受限、占用率不是瓶颈）。目标反向净 FP8 **1.29× → ≥1.6×**。**可迁移出 AMD**——Blackwell 的 MX 格式有同构问题 |
| **C2-7** | **补三个已实测的 attention 缺口**：`d=256` head dim、FP8 attention 性能、Turbo attention backend dispatcher | 20 个主流模型 shape 实测：**`d=256` 是三个后端的无人区**（CK 494–556 TFLOPS = d=128 的一半，aiter Triton 塌到 114–134，HK 无实例），而 **glm5 + qwen3_5_35B 用的就是 d=256**；**FP8 attention 169–454 TFLOPS，40 行中 20 行比 BF16 慢 2–4×**；**Turbo 完全没有 kernel dispatch 层**（官方 TODO，成本低）。另有 16-head 反向（TP=8 单卡份额）掉到约 655 TFLOPS 待用 rocprof 区分调度 vs dQ 原子归约 |
| **C2-8** | **ROCm FP8 grouped-GEMM API 缺口**：`hipblaslt_ext::GroupedGemm` 的 FP8 支持；`torch._scaled_grouped_mm` 的 FNUZ vs E4M3FN 矛盾 | 穷举全部 (opA,opB,dtype) 组合验证：ROCm 7.1 上只有 BF16 grouped GEMM 可用。`torch` API 不可用是因为校验层要 `float8_e4m3fnuz` 而底层 CK kernel 要 `float8_e4m3fn`。"一旦可用，单次 API 调用即消除全部 dispatch 开销" |

### 阶段 C3 — 规模化可靠性（2027 Q2 – Q3）

> slab 的 10k GPU 差距分析把这一类评为**最高影响 × 最低覆盖**。当前 Primus 的答案是人工 SOP。

| ID | 条目 | 依据 |
|---|---|---|
| **C3-1** | **把 goodput / ETTR 立为一等指标**，并建自愈层：快速/异步/内存内 checkpoint、故障自动检测 + 闭环节点池、无需全量重启的弹性重配、straggler 自动检测与驱逐、**SDC 防护**；Projection 增加 checkpoint IO 与恢复时间建模 | 10k 卡规模下 *"goodput = MFU × ETTR"* 决定成败，而 Primus 现在是人工节点驱逐 / 人工 GC / NUMA 绑定 / 三段式 smoke-steady-long 冒烟。参考 MTBF：Llama 3 在 16,384×H100 上 **54 天 419 次意外中断**。**SDC 是完全未跟踪的一层**，而 ByteDance 有两篇 OSDI'26（其一建立在 3500 万 GPU 小时上）。对标：ByteCheckpoint、Gemini、Unicron、Oobleck、Meta Monarch、Decoupled DiLoCo（高故障率下 goodput **88% vs 纯数据并行 27%**，且**混用不同硬件代次不掉性能——直接对应 AMD 客户 MI300X/MI325X/MI355X 混部的现实**） |
| **C3-2** | **运行时动态专家放置/迁移**（或容量感知调度），与 DeepEP / sync-free 路径协同以免迁移重新引入 CPU 同步 | aux loss 只修**统计**均衡，不修**瞬时**热点；10k 规模下一个热专家就是全局等待。对标 **UltraEP**（机架级逐 microbatch 逐 layer 精确重均衡，不均衡度 1.3–4 → 1.0，训练 1.42×，**且明确点名 AMD Helios**）。⚠ 反证：**DODOCO** 在 5 个真实 MoE checkpoint 上推翻两个假设——路由不均衡无法在系统层完全纠正、mock-token benchmark 不代表生产路由；**ForeMoE** 指出预训练均衡方法在 RL 后训练中直接失效（统计量从 step 级降到 micro-step 级） |
| **C3-3** | **拓扑感知通信**：rail-aware EP/DP rank 放置 + 层级化 all-to-all（节点内 XGMI 聚合、节点间 IB），取代手工 `NCCL_SOCKET_IFNAME`/`NCCL_IB_HCA` 绑定 | *"10k 规模下理论带宽会被 hash 冲突打折。"* 对标 Alibaba HPN、Meta RoCE@Scale、OpenAI/Microsoft MRC+SRv6（800 Gb/s 拆 8×100 Gb/s 提升交换机 radix，**约 131,000 GPU 只需两层 Clos**，已贡献 OCP）、UBEP 层级化 token 调度（**两跳延迟 = 本地 11.5×**） |
| **C3-4** | **推 RCCL 向 NCCL 2.28 对齐**：Copy Engine collectives（把 SM 从 alltoall/allgather 里释放）+ device API（kernel 内发起通信）；并在已有 rocSHMEM + mori SDMA 之上暴露框架可直接消费的 API 面 | **RCCL 架构上无法 overlap**（collective 语义 + host 发起），三种方式验证过；NVIDIA 在 NCCL 2.28 解决；ROCm 7.2 的公开 RCCL 进展只到"4-NIC 拓扑感知 + NCCL 2.28 算法 backport"。*"AMD 已有 rocSHMEM 和 mori SDMA allgather 做底座，缺的是框架能直接用的 API 面。"* 佐证压力：NVIDIA 正在给 NCCL 加 EP API、华为 StrataCL 与 UBEP 在重写 EP 库、Meta 造 HCCL、Intel 造 PCCL——**"buffer 中心 + 通用 collective"正在被集体质疑** |
| **C3-5** | **低精度通信，重心在 wgrad reduce-scatter 而非参数 all-gather**（2026-09-03 按上游实证重新定向）。**① 参数侧＝追平**：把 v26.5 Flux 路径的 FSDP2 fp8 all-gather 泛化到 MoE 与稠密主线，对标 NVIDIA 原生 `MXFP8Tensor`/`NVFP4Tensor` 分片 + packed 4-bit all-gather。**② 梯度侧＝真缺口，也是差异化所在**：为 wgrad RS 做块缩放低精度线上格式 | ⚠ **两处旧判断已修正**。(a)"量化 collective 无人认领"**不准确**——NVIDIA 已用 GTP 的 native FP8/NVFP4 param gather 把**参数侧**认领得很彻底，所以参数侧是追平不是差异化。(b)"A2A + 本地 FP32 归约是我们该占的结构"**也不准确**——上游已出货 `--gtp-remat-reduce-scatter-with-fp32-accumulation`（`megatron/core/distributed/reduce_scatter_with_fp32_accumulation.py`，文档原话 *"eliminating that accumulation error **for the same bytes on the wire**"*，即修精度不修带宽），DP 轴还有孪生 flag；ZeRO++ qgZ（[2306.10209](https://arxiv.org/abs/2306.10209), 2023）更早发表同一结构且多 2-hop。**真缺口是格式**：通信库无块缩放 dtype（NCCL 只有 `ncclFloat8e4m3/e5m2`，MXFP8/NVFP4 AllReduce 是未实现 RFE #2199）。真约束是**尾数宽度而非动态范围**——加 W 个数需约 `log₂W` 位额外尾数（W=72 → 6.2 位 vs FP8 E4M3 的 3 位、FP4 的 1 位），正确表述是"累加器必须永远比线上格式宽"。**AMD 没有 NVLS，反使 A2A 重构成为拿到 FP32 精确归约的唯一途径**（GB200 上它只是备选，对称池优先）。收益按拓扑分层：机架内 HBM:线速 ≈ 5.4–6.5 < 门槛 9.5（**亏**），跨机架 ≈ 33–39（**盈 3.5–4×**）——**打跨机架**。地基取 **SDP4Bit**（在 Megatron 内、开源、验到 6.7B/18B、两级结构正合 Helios），位分配借 DynamiQ（该项单独 3.5–5.1×，超其余三项之和），**不要**从 GIFT 的几何变换起步（同表里直接欧氏 FP8 反而快 3.19 pt）。先做两件便宜事：`确定性 Hadamard + FP8 wgrad RS` 对照组、以及补上 GIFT 省掉的"欧氏 + EF"消融。详见 [`knowledge/systems/mi455x-parallelism-strategy-reference.md`](../../knowledge/systems/mi455x-parallelism-strategy-reference.md) §5.6 |
| **C3-6** | **MI300A APU 统一内存的 ROCm CPU offload 优化器** | Microsoft SuperOffload(ASPLOS'26) **明确把 MI300A 列为目标平台**，但 GraceAdam（比 PyTorch CPU Adam 快 3×）**只为 Grace 实现**。*"MI300A 的 APU 统一内存在架构上比 GH200 的 NVLink-C2C 更激进，却没有对应的 ROCm 侧 CPU 优化器实现。"* |
| **C3-7** | **全迭代 HIP graph 捕获 + 统一 DP/PP/TP/EP 通信重叠调度器** | 是每步八个决策点中的第八个、**当前 0 行**，且*"无法作为 monkey-patch 干净实现"*（捕获一步需要拥有它的控制流）。前置条件：plan 中**不含数据相关控制流**，故必须排在 IR 合并与精度显式化之后。NVIDIA 数据显示与同步消除结合后收益很大：**DeepSeek-V3 +8%、GPT-OSS +93%**；AMD 自己 MLPerf Flux 的 HIP graph 捕获拿到 +5.3%。⚠ 与 C3-1/C3-2 有张力：EEP 指出 graph 烘焙的路由元数据是弹性的障碍，**roadmap 必须显式排序** |

### 阶段 C4 — 执行层收拢与研究线（2027 Q3 起，与 C1 并行准备）

| ID | 条目 | 说明 |
|---|---|---|
| **C4-1** | **立项 "Primus-Runtime"**：定位为把约 33,000 行既有代码从三个 backend 命名空间**搬迁**出来，而非造新框架。六模块 R1–R6，C0–C3 分期，以 C1 的上游接受/拒绝信号作为显式 go/no-go | 借 TE 的**组织位置**（横向层，消费者是上游框架）但不借其技术范围——**Primus-Turbo 已经是 AMD 的 TE** |
| **C4-2** | **三套 pipeline 调度栈收敛为一个 plan/execute 运行时**（前序文档的 "P0"），含 `R = RELOAD` vs `R = RECOMPUTE` 改名、吸收 legacy 的细粒度 offload 算子 | 三栈并存（2,277+1,085 / 7,542 / 79 行）、两套 IR、**一个算子名含义相反**、重复已泄漏到用户配置面（`v-half`/`v-min`）。**多数系统级 roadmap 项都排在它之后。** 验收：对 4 条命名 recipe 零吞吐回归且 Projection 误差仍 ≤1.4%。⚠ 可能是组织边界而非技术选择——若 `primuspipe` 与 `zerobubble` 属不同 owner，阻力主要是协调 |
| **C4-3** | **消除重复的 `sdma_symm_mem_collectives`**（305 行 → 一份），定义通信后端接口并让 TorchTitan 消费 | 一个 AMD 硬件能力被实现两遍且**零共享代码**（megatron 142 + torchtitan 163 行）。TorchTitan 非 patch 代码总量只有 1,201 行，即 33k 行一行没复用。**这是证明跨后端复用可行的最便宜、最直观的一击** |
| **C4-4** | **删掉两份冻结的 Megatron MoE 快照**（`deprecated_20251209/`、`deprecated_2caa681a1/`，3,145 行） | *"因为自己的 MoE 执行逻辑无处安放，唯一选择是整文件 vendor 上游再改；上游每变一次就冻结一代。**这笔债在方案 A 下永远还不掉**"* |
| **C4-5** | **研究线**（长期挂账，详见前序文档）：通信感知 top-k 路由（`score = affinity − λ(t)×comm_cost`，估计总通信量降 20–40%，综合评分最高）、AMD-FSEP（XGMI 直读替代 ReduceScatter，估计 2–3×，⚠ 需对照 HipKittens §7.1 重验波前特化）、反向通信调度（拆 dX/dW，估计 +5–15% 且**只需调度框架工作无新 kernel**，Primus 已有 2,107 行相关件且 wgrad 算子已在 IR 中）、MoE 原生 IR、torch.compile MoE pass、在线自适应调度器 | — |
| **C4-6** | **决定是否把可修改 kernel 底座（HipKittens fork/vendor）作为常设 headcount** | *"承接它意味着事实上成为一个 kernel 底座的维护者……**这不是附带成本，是走 megakernel 路线的入场费。** 不付就意味着每次未来的融合都要等别人的排期。"* 供给侧论据：**AITER 与 Turbo 内的 FlyDSL attention 同源**，那个方向优先级是推理，而*"我们背训练 KPI 却在任何训练关键路径上都不拥有可修改实现"*——给了供应商对任何融合路线的**架构否决权**。缓解：HK 是 MIT、接受外部 PR（已合并 AMD 与社区贡献，细到 `ds_read_b64_tr` 与 `mubuf ops` 指令支持），其 `training/llama/.../attentions/` 已是可插拔后端抽象且有 `aiter.py` 对照臂。**杀死该项目的单一变量**：attention/GEMM 供给排期变得可预测，或拿到向其上游提交代码的通道 |

### 贯穿性工作

| ID | 条目 | 说明 |
|---|---|---|
| **X-1** | **持续、可复现、带 trace 的公开性能记录，并恢复周报节奏** | *"AMD 的商业问题不是框架看起来像个 wrapper——而是没人相信 AMD 能训前沿模型，因为公开证据缺失。一份持续、可复现、带 trace 的公开性能记录极其便宜。"* 现状：只有零散 ROCm 博客；**周报在 2026-04 之后停了**，被称为*"最便宜的复利"*。可复现基准资产已存在（`bench_turbo_moe_e2e.py`，无依赖，跟踪出货配置） |
| **X-2** | **清理债务**：约 14,900 行 `*_deprecated` 目录、9 套并行 DSA kernel 后端（无选择与退役策略）、125 处 `NotImplementedError`（`peft/utils.py` 与 `peft/lora_layers.py` 各 4 处 → LoRA 有未实现分支）、`docs_deprecated/`（23 文件）、`pyproject.toml` 声明 MIT 而 `LICENSE` 是 Apache 2.0、版本号不一致（`v26.2`/`v26.2.0rc1`/`v26.5.0`/`v26.6.0` 并存，`primus/__init__.py` 写 `26.5` 而 tag 是 `v26.6.0`） | — |
| **X-3** | **把 41 个配置/UX 类 patch 移出 patch 层**进 `primus/configs/modules/megatron`；3 个 ROCm workaround 推上游并立规矩"ROCm 修复必须有对应上游 PR"；每个剩余 patch 给出 `backend_versions=["<0.17"]` 形式的到期条件 | 42% 的 patch 没有技术深度；ROCm workaround 数量应趋零；`turbo/te_spec_provider_patches.py` 是自退役 patch 的现成先例 |
| **X-4** | **文档与代码一致性**：`backend-patch-notes.md` 称 MaxText 无 patch（实为 2–3 个）、只列 1 个 TorchTitan 开关（实为 21–22 个）；`architecture.md` 列 5 个 adapter（实为 8 个）且仍描述已移除的 legacy 运行时选择；model matrix 漏 DeepSeek-V4 / Kimi K3 / Gemma4 | patch-explorer skill 自己都在教读者"以代码为准"——这本身就是信号 |

---

## 4. 风险与护栏

以下是调研中**反对本 roadmap 部分内容**的证据，必须作为护栏读，不能省略。

| 风险 / 反证 | 影响的条目 |
|---|---|
| **融合持久化 kernel 在文献中被直接批评**（资源刚性划分 + 软件轮询）——UBEP §6 是*"ROCmoe 路线必须正面回答的论文"*；ExpertPlex 另证自适应持久 kernel 优于静态空间划分 | C2-6, C4-5 |
| **Alibaba 生产系统拒绝了 Comet 式融合**（后端耦合），并实测 **EP 通信 kernel 占约 20 SM 造成 10–20% 变慢**；Motif 3 独立佐证*"规模上消除 expert 权重 All-Gather 优于隐藏它"*。即**通信应被消除或卸载，而非仅仅重叠**。Meta 的答案是硬件（MTIA 300 消息引擎），AMD 只能软件解 | C2-6, C3-4 |
| **多机 RDMA 可吃掉整个单机 megakernel 优势**——Perseus 实测 8 节点最多 **10× 退化**，*"节点内不存在此问题"*。而本工作区验证过的一切都是单机 8× MI355X | C2-6, C4-5 |
| **cost model 尾部误差会翻转 MILP 排序**——Tessera 报告 **15% 尾误差即反转排名**，为任何 predicate/projection 驱动的决策设了硬精度门槛 | C1-7, C4-2, C4-5 |
| **AutoMegaKernel 的负面结果**：megakernel 瓶颈是 **per-tile 跨 SM 同步，且在带宽最高的训练级芯片上最严重**——恰是目标硬件 | C4-5, C4-6 |
| **按 token 数分桶的静态 kernel dispatch 是错的**——DA-MoE 显示最优融合 MoE kernel 随路由偏斜与 token 数变化，应做 GPU 常驻运行时 dispatch | C2-4, C2-7 |
| **CUDA/HIP graph 烘焙的路由元数据与弹性冲突**——EEP 把 EP 成员看作可变存活性问题，并指出 graph 烘焙路由元数据是障碍 | **C3-7 与 C3-1/C3-2 的直接张力，必须显式排序** |
| **hipBLASLt 的"选择优势" vs 单配置特化**：hipBLASLt 靠按 shape 运行时挑 tile 在大 BS 上胜出（−5.8%/−7.9%），但小 BS 上差 1.8–2.1× 且有 hang。融合 kernel 内 shape 编译期已知故特化胜出——**这说明融合路径与库路径有真正不同的最优区间，必须做 predicate 而非全局开关** | C2-4, C2-6 |
| **"84% stall 可回收"是谬误**：MFMA issue 已饱和的 kernel 在 ATT 里显示的 barrier/wait 周期与调度糟糕的 kernel 相同。已在 `fc1_dgrad` 上独立证明——移除同步指令**墙钟不变**，因为 barrier stall 反映的是 epilogue 跨波散兵而非同步成本 | C0-1, C2-6 |
| **三次自我纠错说明调错工作负载有多容易**：`tokens_per_gpu` 读错作废了 14 个优化循环；"FC1 ≈ 1.5 ms"实为 3.247 ms（把重叠上限从 50% 翻到 100%）；"cluster" 路由把 dispatch 压缩约 6×，**绝不可当作 Primus 性能引用**。**任何 roadmap 基准定义必须显式锁定负载语义** | C0-1, X-1 |
| **方案 C 的执行风险**：*"若把这个项目当成'开发一堆组件'来推，它会退化成零件箱——局部最优的组件、没有统一的 plan，最终既无差异化也无产品故事。守住 plan/execute 分离是前提，不是实现细节。"* 且目标架构**未经生产验证**（仅 2 个测试 yaml），迁移必须双栈并行而非切换 | C4-1, C4-2 |
| **FP4 随机性问题未决**：Penn State + AMD 在 MI355X **原生** MXFP4 上测得达到 ppl 3.3 的 token 成本 Fprop 8–9% → +Dgrad 10–11% → **+Wgrad 26–27%**；随机舍入与随机 Hadamard **不收敛**，确定性 Hadamard 恢复到 8–9%。单步 +20% 吞吐端到端只剩 **+9–10%**。**与 NVIDIA 直接冲突**（TE `NVFP4BlockScaling` 默认随机舍入 + RHT）。**无跨厂商独立复现**。且该论文与 MLPerf 博客**不是独立来源**（同团队同流水线），并且**只做稠密、完全没碰 MoE / grouped GEMM** | C3-5 |

---

## 5. 对外叙事（优异点摘要）

给客户/管理层的一页版本，全部有实测支撑：

1. **AMD 上唯一被持续验证的多后端训练框架。** 上游 TorchTitan 已停掉 ROCm CI、删除 ROCm golden、把 ROCm 排除在发版验证之外；上游 Megatron 的 core 里 ROCm 相关代码 1 处、`torch.cuda` 调用 796 处。Primus 是这条路径上唯一有人守的地方。
2. **MoE 融合执行领先。** MegaMoE kernel 级 1.51×、端到端 1.123×，且**与低精度叠加**（MXFP8 下比 BF16 下更划算）并**降低峰值显存**。上游没有对标物。
3. **大规模实证，不是 demo。** DeepSeek-V3 在 1024× MI355X 上 1.095× 基线，1700 连续步中位 22.08 s、93.2% 在 ±2% 内、10.6 小时漂移 1.8%；128 节点保持 32 节点单卡吞吐 98.5%。
4. **训练前就能回答"装得下吗、多快"。** Projection 全部发布案例误差 ≤10%，MoE 案例 1.4%，且有纯 CPU 模式。上游把自己的估算脚本删了。
5. **调度能力上游没有。** zero-bubble / DualPipe 家族在上游 Megatron 是 0 命中，Primus 有 31 个 scheduler 文件的实现。
6. **能发现并根治别人发现不了的静默正确性问题。** DeepEP 的 `kUseCheapFence` 内存序 bug 导致静默错梯度（loss 6.1 vs 4.52），根因定位后修进上游；`schema_check.py` 把一次"所有 DeepSeek 配置都在关着负载均衡训练"的事故变成了 CI 门禁。对企业客户，这比多 5% 吞吐更值钱。

---

## 6. 来源

| 输入 | 报告 | 基线 |
|---|---|---|
| slab 调研沉淀 | `/tmp/primus_roadmap/01_slab_research.md` | `cfd02bd`, 2026-08-24 |
| 上游 TorchTitan | `/tmp/primus_roadmap/02_torchtitan.md` | `c4ad49b5c`, 2026-09-02 |
| 上游 Megatron-LM | `/tmp/primus_roadmap/03_megatron.md` | `fb7ba4c83`, 2026-09-02（core 0.20.0-dev） |
| Primus 现状 | `/tmp/primus_roadmap/04_primus_current.md` | `6e2b114b`, 2026-08-30 |
